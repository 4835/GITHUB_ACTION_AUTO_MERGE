# name: automerge
# on:
#   pull_request_review:
#     types: [submitted]
# jobs:
#   pr-automerge:
#     if: github.event.review.state == 'approved'
#     runs-on: ubuntu-latest
#     steps:
#       - name: PR approved
#         run: |
#           echo "PR approved" 
